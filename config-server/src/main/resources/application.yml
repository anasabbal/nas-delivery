server:
  port: 8888

spring:
  profiles:
    active: config
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/anasabbal/config-repo.git
        default-label: master